<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title>List of Genres</title>
</head>
<body>
    <h1>List of Genres</h1>
    <ul>
        <% genres.forEach(function(genre) { %>
            <li><%= genre.name %></li>
        <% }); %>
    </ul>
</body>
</html>
